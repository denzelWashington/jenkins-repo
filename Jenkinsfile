pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                echo 'Tests OK'
            }
        }

        stage('Build') {
            steps {
                echo 'Build OK'
            }
        }
    }
}